## Handoff: team-plan → team-exec

- **Decided**: 6 building interiors built as recognizable rooms using Kenney RPG Urban tiles (`assets/external_v3/`, CC0, 16×16px, indices in `assets/external_v3/TILE_INDEX.md`). Interior data stored as JS globals (`globalThis.INTERIORS[placeId]`) in per-place files `data/interiors/<id>.js`, loaded via `<script>` tags (NOT fetch — matches `places.js` pattern, avoids file:// issues). `placeDef()` (game.js:64-91) rewritten to consume `INTERIORS[place.id]`. Interior props mirror each place's `lookMission` items.
- **Sequencing**: Foundation (worker-1) runs FIRST and alone — it defines the `INTERIORS` schema, rewrites `placeDef()`+renderer, adds 6 `<script>` tags to index.html, builds the LIBRARY fully as the template, and writes minimal valid STUBS for the other 5 (so game never breaks). THEN 5 interior workers fill their own isolated file in parallel (no merge conflicts; index.html already wired).
- **Rejected**: Tiled manual authoring (user: agent generates, not hand-design); JSON+fetch (file:// CORS risk); single shared data/interiors.js (parallel merge conflicts).
- **Risks**: (1) 16px Urban tiles vs 32px world `TS` — renderer must scale/handle. (2) RPG Urban is an *outdoor* pack — true interior furniture (bookshelf/desk/shelf/counter) may be sparse; allow synthesizing a few 16px furniture tiles via canvas/graphics where the sheet lacks them. (3) Must not regress existing loop (dialogue/voice/photo-mission/treasure/result) — placeDef still returns npc/npcCell/portals/spawn/collide.
- **Files**: game.js (placeDef + interior renderer), index.html (6 script tags), data/interiors/*.js (6 new). Context: .omc/plans/map-osm-integration-handoff.md §12.5, assets/external_v3/TILE_INDEX.md.
- **Remaining**: team-exec foundation then 5 interiors; team-verify = local server + no console errors + each place enters & renders, regression check.
